{
  "projectId": "230c9097-1a5c-4f53-be2c-12858f7096b2",
  "shape": "off-script",
  "note": "This Python static-site repo is NOT the source of the Driftwood Design System project (which has React components + UI-kit screens authored elsewhere). Runs from here do a TARGETED reference refresh only: reference/* mirror files from docs/. Never full-sync (would delete the React components). tokens/, components/, guidelines/, templates/, ui_kits/ are curated DS artifacts — leave untouched."
}
